"""What the court used to do for itself, kept where it can be read (Task 2).

Nothing here runs. Each module is a system the kernel took over, archived whole
rather than deleted, because the migration's claim is that the kernel does the
same job and the only way to check a claim like that is against the thing it
replaced. `docs/TASK_2_TODO.md` names what crossed and when.
"""
